# LLMCache-Bench — runtime requirements (tight, project-focused)
# Purpose: Evaluate GPTCache + FAISS semantic caching on PlatinumBench via local Ollama.
# Note: Install with `pip install -r requirements.txt` for full functionality including
# custom semantic caching strategies and visualization scripts.

# Config + CLI tooling
PyYAML>=6.0

# Data loading (Hugging Face datasets is REQUIRED for PlatinumBench)
datasets>=4.0.0

# Caching stack (REQUIRED)
gptcache>=0.1.44
faiss-cpu>=1.7.4
numpy>=1.26
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.1.0
langchain-ollama>=0.1.0

# Scripts (aggregation + plotting)
pandas>=2.0
matplotlib>=3.7

# Model transport: simple HTTP client to local Ollama
requests>=2.31

# Testing
pytest>=8.0

# Progress tracking
tqdm>=4.65

# ML dependencies (required by gptcache and custom_semantic strategy)
onnxruntime>=1.14.1
transformers>=4.21.0
sentence-transformers>=2.2.0

# Visualization dependencies (for plotting scripts)
seaborn>=0.12.0
