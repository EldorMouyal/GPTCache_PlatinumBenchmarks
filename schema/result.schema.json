{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://llmcache-bench.local/schema/result.schema.json",
  "title": "LLMCache-Bench Run Result",
  "type": "object",
  "additionalProperties": false,
  "required": ["run", "model", "dataset", "cache", "metrics", "items", "output"],
  "properties": {
    "run": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "seed", "started_at", "completed_at"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "seed": { "type": "integer" },
        "notes": { "type": "string" },
        "started_at": { "type": "string", "format": "date-time" },
        "completed_at": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["success", "partial", "failed"] }
      }
    },

    "model": {
      "type": "object",
      "additionalProperties": false,
      "required": ["provider", "name", "params"],
      "properties": {
        "provider": { "type": "string" },
        "name": { "type": "string" },
        "params": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "temperature": { "type": "number" },
            "top_p": { "type": "number" },
            "max_tokens": { "type": "integer" }
          }
        }
      }
    },

    "dataset": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "split", "slice", "count_processed"],
      "properties": {
        "name": { "type": "string" },
        "split": { "type": "string" },
        "slice": {
          "type": "object",
          "additionalProperties": false,
          "required": ["start", "limit"],
          "properties": {
            "start": { "type": "integer", "minimum": 0 },
            "limit": { "type": "integer", "minimum": 1 }
          }
        },
        "count_processed": { "type": "integer", "minimum": 0 }
      }
    },

    "cache": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode"],
      "properties": {
        "mode": { "type": "string", "enum": ["none", "vanilla-exact", "vanilla-approx", "extended"] },
        "stats": {
          "type": "object",
          "additionalProperties": false,
          "required": ["hits", "misses"],
          "properties": {
            "hits": { "type": "integer", "minimum": 0 },
            "misses": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },

    "metrics": {
      "type": "object",
      "additionalProperties": false,
      "required": ["latency_mean_sec", "latency_p95_sec", "latency_p99_sec", "cache_hit_rate", "correctness"],
      "properties": {
        "latency_mean_sec": { "type": "number", "minimum": 0 },
        "latency_p95_sec":  { "type": "number", "minimum": 0 },
        "latency_p99_sec":  { "type": "number", "minimum": 0 },
        "cache_hit_rate": { "type": "number", "minimum": 0, "maximum": 1 },
        "bad_cache_hit_rate": { "type": "number", "minimum": 0, "maximum": 1 },
        "cache_accuracy": { "type": "number", "minimum": 0, "maximum": 1 },
        "cache_statistics": {
          "type": "object",
          "additionalProperties": false,
          "required": ["total_queries", "cache_hits", "cache_misses", "bad_cache_hits", "good_cache_hits"],
          "properties": {
            "total_queries": { "type": "integer", "minimum": 0 },
            "cache_hits": { "type": "integer", "minimum": 0 },
            "cache_misses": { "type": "integer", "minimum": 0 },
            "bad_cache_hits": { "type": "integer", "minimum": 0 },
            "good_cache_hits": { "type": "integer", "minimum": 0 },
            "hit_rate": { "type": "number", "minimum": 0, "maximum": 1 },
            "bad_hit_rate": { "type": "number", "minimum": 0, "maximum": 1 },
            "cache_accuracy": { "type": "number", "minimum": 0, "maximum": 1 }
          }
        },
        "throughput_qps": { "type": "number", "minimum": 0 },
        "correctness": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    },

    "items": {
      "type": "array",
      "minItems": 0,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "prompt", "response", "latency_ms", "cached"],
        "properties": {
          "id": { "type": "string" },
          "prompt": { "type": "string" },
          "response": { "type": "string" },
          "gold": { "type": "string" },
          "latency_ms": { "type": "number", "minimum": 0 },
          "cached": { "type": "boolean" },
          "cache_hit_score": { "type": "number" },
          "error": { "type": "string" }
        }
      }
    },

    "output": {
      "type": "object",
      "additionalProperties": false,
      "required": ["dir", "file"],
      "properties": {
        "dir": { "type": "string" },
        "file": { "type": "string" }
      }
    }
  }
}
